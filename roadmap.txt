github token: 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


roadmap:

0. define ngrx store
define query calls:
1. all repos (with limit of 50): 
{
  search(query: "is:public", type: REPOSITORY, first: 5) {
    repositoryCount
    pageInfo {
      endCursor
      startCursor
    }
    edges {
      node {
        ... on Repository {
          name
          owner {
            avatarUrl
          }
          nameWithOwner
          description
          createdAt
          updatedAt
          homepageUrl
        }
      }
    }
  }
}
2. scrape the repo website on click for list of all contributors:
	- (async () => {
  const response = await fetch('https://github.com/martyjs/marty/graphs/contributors');
  const text = await response.text();
  console.log(text);
})(); --https://medium.com/nerd-for-tech/angular-11-insert-external-html-in-your-own-website-web-scraping-ff78f2540c4b
	- get info on names, avatar and other information

3. get detail information on single users
{
  user(login: "ashutosh1919"){
    name,
    avatarUrl,
    company,
    email,
    location,
    websiteUrl
    repositories (first: 3, orderBy:{field:STARGAZERS, direction: DESC}) {
      edges {
        node {
          name,
          url
        }
      }
    }
  }
}
4. design the overall structure, including router - use welcome screen from previous project
5. design the table for all users
	- implement lazy loading (angular material?)
6. design the single user card for loading the users
7. implement a way to display the cards adequately